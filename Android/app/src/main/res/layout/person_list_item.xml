<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/friend_item"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >

    <ImageView android:id="@+id/player_avatar"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="6dp"
        android:layout_marginRight="6dp"
        android:src="@drawable/avatar_stock"
        android:scaleType="fitCenter" />

    <TextView android:id="@+id/person_name_textview"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/player_avatar"
        android:layout_toLeftOf="@+id/rank_textview"
        android:gravity="center_vertical"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:text="Person Name" />

    <TextView android:id="@+id/person_wealth_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5dp"
        android:layout_below="@+id/person_name_textview"
        android:layout_toRightOf="@+id/player_avatar"
        android:textColor="@android:color/black"
        android:text="Wealth:" />

    <TextView android:id="@+id/person_wealth_textview"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginLeft="5dp"
        android:layout_below="@+id/person_name_textview"
        android:layout_toLeftOf="@+id/rank_textview"
        android:layout_toRightOf="@+id/person_wealth_label"
        android:textColor="@android:color/black"
        android:text="$Worth" />

    <TextView android:id="@+id/rank_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/player_avatar"
        android:layout_alignBottom="@+id/player_avatar"
        android:text="1"
        android:gravity="center_vertical"/>

</RelativeLayout>